import React from 'react'
import { SectionTitle } from '../shared'

/**
 * Introduction section for the component showcase page
 */
const ShowcaseIntro = () => {
  return (
    <div className="text-center">
      <SectionTitle 
        title="Core Components" 
        subtitle="Explore the available components in the Agilit-e Core library. Each component can be easily imported and incorporated into your React application."
      />
    </div>
  )
}

export default ShowcaseIntro 